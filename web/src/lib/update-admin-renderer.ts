const css = `
:root{color-scheme:light dark;--bg:#f7f4ef;--panel:#fffaf2;--ink:#17130d;--muted:#756b5d;--line:#ded4c4;--accent:#111827;--disabled:#e7dfd2;--good:#136f3f;--warn:#9a5a00}*{box-sizing:border-box}body{margin:0;font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;background:radial-gradient(circle at top left,#fff 0,#f7f4ef 42%,#eee6d8 100%);color:var(--ink)}main{max-width:1180px;margin:0 auto;padding:32px 20px 48px}.top{display:flex;justify-content:space-between;gap:20px;align-items:flex-start;margin-bottom:24px}.eyebrow{font-size:12px;letter-spacing:.18em;text-transform:uppercase;color:var(--muted);margin:0 0 8px}h1{font-size:34px;line-height:1.1;margin:0 0 10px}.muted{color:var(--muted);line-height:1.7}.card{background:color-mix(in srgb,var(--panel) 92%,white);border:1px solid var(--line);border-radius:22px;box-shadow:0 20px 60px #61410d14;padding:22px}.login{display:grid;gap:12px;max-width:420px}.login input{width:100%;border:1px solid var(--line);border-radius:14px;padding:12px 14px;font:inherit;background:#fff}.button{border:0;border-radius:999px;background:var(--accent);color:white;padding:11px 16px;font-weight:650;cursor:pointer}.button.secondary{background:#fff;color:var(--ink);border:1px solid var(--line)}.button:disabled{background:var(--disabled);color:#8d8374;cursor:not-allowed}.sources{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:14px;margin:20px 0}.source{display:flex;flex-direction:column;gap:10px;min-height:170px}.source h2{font-size:18px;margin:0}.badge{width:max-content;border:1px solid var(--line);border-radius:999px;padding:4px 9px;font-size:12px;color:var(--muted)}.badge.ready{color:var(--good);border-color:color-mix(in srgb,var(--good) 35%,var(--line))}.badge.strict{color:#7c2d12;border-color:#fdba74}.badge.routine{color:#166534;border-color:#86efac}.actions{display:flex;gap:10px;flex-wrap:wrap;margin-top:auto}.workspace{display:grid;grid-template-columns:340px 1fr;gap:16px;align-items:start}.log{white-space:pre-wrap;font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:12px;background:#17130d;color:#f8efe0;border-radius:18px;padding:16px;max-height:520px;overflow:auto}.summary{display:block;position:sticky;top:16px}.summary .stat{display:flex;justify-content:space-between;border-bottom:1px solid var(--line);padding-bottom:8px;margin-bottom:10px}.summaryActions{display:flex;gap:10px;flex-wrap:wrap;margin:14px 0}.summary .muted{margin:0}.diffSummary{display:grid;gap:10px;margin-top:12px}.diffGroup{border:1px solid var(--line);border-radius:16px;background:#fff8;padding:12px}.diffGroup summary{cursor:pointer;font-weight:700;display:flex;gap:8px;align-items:center;justify-content:space-between}.fileList{list-style:none;padding:0;margin:10px 0 0;display:grid;gap:6px}.fileList li{display:grid;gap:8px}.fileList button{width:100%;text-align:left;border:1px solid var(--line);border-radius:12px;background:white;padding:8px 10px;cursor:pointer;color:var(--ink)}.fileList button[aria-expanded="true"]{border-color:var(--accent);box-shadow:0 0 0 2px #11182714}.fileMeta{color:var(--muted);font-size:12px}.inlineDiff{margin:0 0 8px 0}.hidden{display:none!important}@media(max-width:880px){.sources,.workspace{grid-template-columns:1fr}.summary{position:static}.top{display:block}}
`;

export function renderUpdateAdminPage(): string {
  return `<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>数据同步 · mddb.dev</title><style>${css}</style></head><body><main><section class="top"><div><p class="eyebrow">Internal Update</p><h1>内部数据同步</h1></div><a class="button secondary" href="/">返回模型广场</a></section><section id="login" class="card login" aria-label="管理员登录"><label><span class="muted">管理员密码</span><input id="password" type="password" autocomplete="current-password" placeholder="输入同步工具密码"></label><button id="unlock" class="button">进入</button><p id="loginMessage" class="muted" role="status"></p></section><section id="console" class="hidden" aria-label="数据源同步控制台"><div class="sources">${renderSourceCards()}</div><div class="workspace"><aside class="card summary"><h2>本次预览</h2><div class="stat"><span>数据源</span><b id="activeSource">—</b></div><div class="stat"><span>变更文件</span><b id="changedFiles">0</b></div><div class="stat"><span>状态</span><b id="runStatus">等待操作</b></div><div class="summaryActions"><button id="apply" class="button" disabled>确认写入全部变更</button><button id="reject" class="button secondary" disabled>放弃本次预览</button></div><p class="muted">确认/放弃以本次预览为单位；models.json 与自研/作者 Provider 暂不拆开单独写入，避免关联数据一半进入仓库。</p></aside><section><div id="diffSummary" class="diffSummary"><p class="muted">点击“预览 diff”后会分为：核心：models.json、核心：自研/作者 Provider、一般：渠道/端点 Provider。需要人工判断时再按文件查看。</p></div></section></div></section><script>${clientJs()}</script></main></body></html>`
}

function renderSourceCards(): string {
  const sources = [
    { id: 'openrouter', name: 'OpenRouter', enabled: true, note: '已接入脚本：抓取模型、端点、页面补充，并生成 registry 数据。' },
    { id: 'models-dev', name: 'models.dev', enabled: false, note: '占位：后续接入为 OpenRouter-first 数据补充。' },
    { id: 'basellm', name: 'BaseLLM', enabled: false, note: '占位：后续接入价格/可用性补充。' },
    { id: 'litellm', name: 'LiteLLM', enabled: false, note: '占位：后续接入模型价格补充。' },
  ]
  return sources.map((source) => `<article class="card source" data-source-id="${source.id}" data-enabled="${source.enabled}"><span class="badge ${source.enabled ? 'ready' : ''}">${source.enabled ? '可点击' : '占位'}</span><h2>${source.name}</h2><p class="muted">${source.note}</p><div class="actions"><button class="button" data-preview="${source.id}" ${source.enabled ? '' : 'disabled'}>预览 diff</button></div></article>`).join('')
}

function clientJs(): string {
  return `
const state={password:'',patchFile:null,source:null,diff:'',activeDiffButton:null};
const $=(id)=>document.getElementById(id);
const escapeHtml=(value)=>String(value).replace(/[&<>"']/g,(char)=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
function resetPreview(){state.patchFile=null;state.diff='';state.activeDiffButton=null;$('apply').disabled=true;$('reject').disabled=true;$('changedFiles').textContent='0';$('runStatus').textContent='等待操作';$('diffSummary').innerHTML='<p class="muted">已放弃本次预览，可重新生成。</p>';}
function extractFileDiff(diff,path){const marker='diff --git a/'+path+' b/'+path;const start=diff.indexOf(marker);if(start<0)return diff||'没有 diff';const next=diff.indexOf(String.fromCharCode(10)+'diff --git ',start+1);return diff.slice(start,next<0?diff.length:next).trim();}
function toggleFileDiff(button){const existing=document.querySelector('.inlineDiff');if(existing)existing.remove();if(state.activeDiffButton&&state.activeDiffButton!==button)state.activeDiffButton.setAttribute('aria-expanded','false');if(state.activeDiffButton===button){button.setAttribute('aria-expanded','false');state.activeDiffButton=null;return;}button.setAttribute('aria-expanded','true');state.activeDiffButton=button;button.closest('li').insertAdjacentHTML('beforeend','<pre class="log inlineDiff">'+escapeHtml(extractFileDiff(state.diff,button.dataset.fileDiff||''))+'</pre>');}
function renderDiffSummary(summary,diff){state.diff=diff||'';state.activeDiffButton=null;const host=$('diffSummary');if(!summary||!Array.isArray(summary.groups)||summary.groups.length===0){host.innerHTML='<p class="muted">没有可分组的变更。</p>';return;}host.innerHTML='<h3>变更摘要</h3>'+summary.groups.map((group,index)=>'<details class="diffGroup" '+(index<2?'open':'')+'><summary><span>'+escapeHtml(group.label)+'</span><span class="badge '+escapeHtml(group.reviewLevel)+'">'+(group.reviewLevel==='strict'?'重点确认':'一般确认')+'</span></summary><ul class="fileList">'+group.files.map((file)=>'<li><button type="button" aria-expanded="false" data-file-diff="'+escapeHtml(file.path)+'"><strong>'+escapeHtml(file.path)+'</strong><br><span class="fileMeta">+'+file.addedLines+' / -'+file.removedLines+' · 按文件查看</span></button></li>').join('')+'</ul></details>').join('');host.querySelectorAll('[data-file-diff]').forEach((button)=>button.addEventListener('click',()=>toggleFileDiff(button)));}
$('unlock').addEventListener('click',async()=>{state.password=$('password').value;const res=await fetch('/api/update/session',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({password:state.password})});if(res.ok){$('login').classList.add('hidden');$('console').classList.remove('hidden');$('loginMessage').textContent=''}else{$('loginMessage').textContent='密码不正确'}});
document.querySelectorAll('[data-preview]').forEach((button)=>button.addEventListener('click',async()=>{const source=button.dataset.preview;state.source=source;state.patchFile=null;$('apply').disabled=true;$('reject').disabled=true;$('activeSource').textContent=source;$('runStatus').textContent='读取中';$('diffSummary').innerHTML='<p class="muted">正在生成摘要…</p>';const endpoint=source==='openrouter'?'/api/update/openrouter/preview':'/api/update/'+source+'/preview';const res=await fetch(endpoint,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({password:state.password})});const data=await res.json().catch(()=>({ok:false,error:'响应不是 JSON'}));if(!res.ok||!data.ok){$('runStatus').textContent='失败';$('diffSummary').innerHTML='';return;}state.patchFile=data.patchFile;$('changedFiles').textContent=String((data.changedFiles||[]).length);$('runStatus').textContent='等待确认';renderDiffSummary(data.summary,data.diff||'');$('apply').disabled=!data.patchFile;$('reject').disabled=!data.patchFile;}));
$('reject').addEventListener('click',resetPreview);
$('apply').addEventListener('click',async()=>{if(!state.patchFile||!state.source)return;$('runStatus').textContent='写入中';const endpoint=state.source==='openrouter'?'/api/update/openrouter/apply':'/api/update/'+state.source+'/apply';const res=await fetch(endpoint,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({password:state.password,patchFile:state.patchFile})});const data=await res.json().catch(()=>({ok:false,error:'响应不是 JSON'}));if(!res.ok||!data.ok){$('runStatus').textContent='写入失败';return;}$('runStatus').textContent='已写入';$('apply').disabled=true;$('reject').disabled=true;});`
}
