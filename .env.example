# Copy to .env.local and fill in local/private values.
# .env.local is intentionally ignored by git.

# Optional but recommended for OpenRouter data refresh scripts.
OPENROUTER_API_KEY=

# Required only when running the internal /update/ data-sync console.
# Use a long random value in deployment.
UPDATE_ADMIN_PASSWORD=
